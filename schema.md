"TABLE_NAME"	"ddl"\n"account_detail"	"CREATE TABLE `account_detail` (
  `id` bigint unsigned NOT NULL auto_increment,
  `header_id` bigint unsigned NOT NULL,
  `type_code` char(1) COMMENT '1=사업자, 2=대표이사, 3=개인',
  `login_type_code` char(2),
  `id_source_type` char(1) NOT NULL DEFAULT '1' COMMENT '1=직접입력, 2=마스터',
  `id_master_id` bigint unsigned,
  `login_id` varchar(255),
  `password_source_type` char(1) NOT NULL DEFAULT '1' COMMENT '1=직접입력, 2=마스터',
  `password_master_id` bigint unsigned,
  `password_enc` varchar(512),
  `authority_code` varchar(100),
  `employee_id` bigint unsigned,
  `memo` text,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `fk_account_detail_header` (`header_id`),
  KEY `idx_account_detail_id_master_id` (`id_master_id`),
  KEY `idx_account_detail_id_source_type` (`id_source_type`),
  KEY `idx_account_detail_password_master_id` (`password_master_id`),
  KEY `idx_account_detail_password_source_type` (`password_source_type`),
  KEY `fk_account_detail_employee` (`employee_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"account_header"	"CREATE TABLE `account_header` (
  `id` bigint unsigned NOT NULL auto_increment,
  `url` varchar(1000) NOT NULL,
  `title` varchar(255) NOT NULL,
  `tags_json` json,
  `delete_yn` char(1) NOT NULL DEFAULT 'N',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"bank_account"	"CREATE TABLE `bank_account` (
  `id` bigint unsigned NOT NULL auto_increment,
  `bank_name` varchar(100) NOT NULL,
  `account_no` varchar(255) NOT NULL,
  `depositor_name` varchar(255) NOT NULL,
  `authority_code` varchar(100),
  `bank_password_enc` varchar(512),
  `internet_banking_id` varchar(255),
  `internet_banking_password_enc` varchar(512),
  `otp_serial_no` varchar(255),
  `project_code` varchar(100),
  `memo` text,
  `use_yn` char(1) NOT NULL DEFAULT 'Y',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"bank_transaction"	"CREATE TABLE `bank_transaction` (
  `id` bigint unsigned NOT NULL auto_increment,
  `account_id` bigint unsigned NOT NULL,
  `transaction_datetime` datetime NOT NULL,
  `deposit_amount` bigint NOT NULL DEFAULT '0',
  `withdraw_amount` bigint NOT NULL DEFAULT '0',
  `balance_amount` bigint,
  `description` varchar(1000),
  `settlement_reflected_yn` char(1) NOT NULL DEFAULT 'N',
  `raw_data_json` json,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `fk_bank_transaction_account` (`account_id`),
  KEY `idx_bank_transaction_datetime` (`transaction_datetime`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"base_code"	"CREATE TABLE `base_code` (
  `id` bigint unsigned NOT NULL auto_increment,
  `group` varchar(100) NOT NULL,
  `key` varchar(100) NOT NULL,
  `value` varchar(255) NOT NULL,
  `description` text,
  `use_yn` char(1) NOT NULL DEFAULT 'Y',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `uk_base_code_group_key` (`group`, `key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"batch_log"	"CREATE TABLE `batch_log` (
  `id` bigint unsigned NOT NULL auto_increment,
  `batch_group` varchar(100) NOT NULL,
  `batch_seq` bigint NOT NULL,
  `title` varchar(255) NOT NULL,
  `executed_date` datetime NOT NULL,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `uk_batch_log_group_seq` (`batch_group`, `batch_seq`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"card_sales_import"	"CREATE TABLE `card_sales_import` (
  `id` bigint unsigned NOT NULL auto_increment,
  `approval_date` date,
  `approval_datetime` datetime,
  `card_company` varchar(100),
  `merchant_name` varchar(255),
  `approval_amount` bigint NOT NULL DEFAULT '0',
  `settlement_reflected_yn` char(1) NOT NULL DEFAULT 'N',
  `raw_data_json` json,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `idx_card_sales_import_approval_date` (`approval_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"employee"	"CREATE TABLE `employee` (
  `id` bigint unsigned NOT NULL auto_increment,
  `employee_no` varchar(50) NOT NULL,
  `password_hash` varchar(255) NOT NULL,
  `resident_registration_no_front` char(6),
  `resident_registration_no_back_enc` varchar(512),
  `name` varchar(100) NOT NULL,
  `english_name` varchar(100),
  `nickname` varchar(100),
  `department_code` varchar(100),
  `position_code` varchar(100),
  `authority_code` varchar(100),
  `phone` varchar(50),
  `email` varchar(255),
  `bank_name` varchar(100),
  `bank_account_no` varchar(255),
  `address` varchar(500),
  `join_date` date,
  `resign_date` date,
  `delete_yn` char(1) NOT NULL DEFAULT 'N',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `idx_employee_authority` (`authority_code`),
  KEY `idx_employee_name` (`name`),
  KEY `idx_employee_phone` (`phone`),
  KEY `uk_employee_employee_no` (`employee_no`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"employee_contract"	"CREATE TABLE `employee_contract` (
  `id` bigint unsigned NOT NULL auto_increment,
  `employee_id` bigint unsigned NOT NULL,
  `written_date` date,
  `contract_start_date` date NOT NULL,
  `contract_end_date` date,
  `annual_salary` bigint,
  `file_path` varchar(1000),
  `delete_yn` char(1) NOT NULL DEFAULT 'N',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `fk_employee_contract_employee` (`employee_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"expense"	"CREATE TABLE `expense` (
  `id` bigint unsigned NOT NULL auto_increment,
  `target_ym` char(6) NOT NULL,
  `category_code` varchar(100),
  `project_code` varchar(100),
  `amount` bigint NOT NULL DEFAULT '0',
  `memo` text,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `idx_expense_target_ym` (`target_ym`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"id_master"	"CREATE TABLE `id_master` (
  `id` bigint unsigned NOT NULL auto_increment,
  `title` varchar(255) NOT NULL,
  `login_id` varchar(255) NOT NULL,
  `use_yn` char(1) NOT NULL DEFAULT 'Y',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"password_master"	"CREATE TABLE `password_master` (
  `id` bigint unsigned NOT NULL auto_increment,
  `authority_code` varchar(100),
  `title` varchar(255) NOT NULL,
  `password_enc` varchar(512) NOT NULL,
  `use_yn` char(1) NOT NULL DEFAULT 'Y',
  `immutable_yn` char(1) NOT NULL DEFAULT 'Y',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"sales"	"CREATE TABLE `sales` (
  `id` bigint unsigned NOT NULL auto_increment,
  `target_ym` char(6) NOT NULL,
  `category_code` varchar(100),
  `project_code` varchar(100),
  `amount` bigint NOT NULL DEFAULT '0',
  `memo` text,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `idx_sales_target_ym` (`target_ym`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"TABLE_NAME"	"ddl"\n"account_detail"	"CREATE TABLE `account_detail` (
  `id` bigint unsigned NOT NULL auto_increment,
  `header_id` bigint unsigned NOT NULL,
  `type_code` char(1) COMMENT '1=사업자, 2=대표이사, 3=개인',
  `login_type_code` char(2),
  `id_source_type` char(1) NOT NULL DEFAULT '1' COMMENT '1=직접입력, 2=마스터',
  `id_master_id` bigint unsigned,
  `login_id` varchar(255),
  `password_source_type` char(1) NOT NULL DEFAULT '1' COMMENT '1=직접입력, 2=마스터',
  `password_master_id` bigint unsigned,
  `password_enc` varchar(512),
  `authority_code` varchar(100),
  `employee_id` bigint unsigned,
  `memo` text,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `fk_account_detail_header` (`header_id`),
  KEY `idx_account_detail_id_master_id` (`id_master_id`),
  KEY `idx_account_detail_id_source_type` (`id_source_type`),
  KEY `idx_account_detail_password_master_id` (`password_master_id`),
  KEY `idx_account_detail_password_source_type` (`password_source_type`),
  KEY `fk_account_detail_employee` (`employee_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"account_header"	"CREATE TABLE `account_header` (
  `id` bigint unsigned NOT NULL auto_increment,
  `url` varchar(1000) NOT NULL,
  `title` varchar(255) NOT NULL,
  `tags_json` json,
  `delete_yn` char(1) NOT NULL DEFAULT 'N',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"bank_account"	"CREATE TABLE `bank_account` (
  `id` bigint unsigned NOT NULL auto_increment,
  `bank_name` varchar(100) NOT NULL,
  `account_no` varchar(255) NOT NULL,
  `depositor_name` varchar(255) NOT NULL,
  `authority_code` varchar(100),
  `bank_password_enc` varchar(512),
  `internet_banking_id` varchar(255),
  `internet_banking_password_enc` varchar(512),
  `otp_serial_no` varchar(255),
  `project_code` varchar(100),
  `memo` text,
  `use_yn` char(1) NOT NULL DEFAULT 'Y',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"bank_transaction"	"CREATE TABLE `bank_transaction` (
  `id` bigint unsigned NOT NULL auto_increment,
  `account_id` bigint unsigned NOT NULL,
  `transaction_datetime` datetime NOT NULL,
  `deposit_amount` bigint NOT NULL DEFAULT '0',
  `withdraw_amount` bigint NOT NULL DEFAULT '0',
  `balance_amount` bigint,
  `description` varchar(1000),
  `settlement_reflected_yn` char(1) NOT NULL DEFAULT 'N',
  `raw_data_json` json,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `fk_bank_transaction_account` (`account_id`),
  KEY `idx_bank_transaction_datetime` (`transaction_datetime`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"base_code"	"CREATE TABLE `base_code` (
  `id` bigint unsigned NOT NULL auto_increment,
  `group` varchar(100) NOT NULL,
  `key` varchar(100) NOT NULL,
  `value` varchar(255) NOT NULL,
  `description` text,
  `use_yn` char(1) NOT NULL DEFAULT 'Y',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `uk_base_code_group_key` (`group`, `key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"batch_log"	"CREATE TABLE `batch_log` (
  `id` bigint unsigned NOT NULL auto_increment,
  `batch_group` varchar(100) NOT NULL,
  `batch_seq` bigint NOT NULL,
  `title` varchar(255) NOT NULL,
  `executed_date` datetime NOT NULL,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `uk_batch_log_group_seq` (`batch_group`, `batch_seq`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"card_sales_import"	"CREATE TABLE `card_sales_import` (
  `id` bigint unsigned NOT NULL auto_increment,
  `approval_date` date,
  `approval_datetime` datetime,
  `card_company` varchar(100),
  `merchant_name` varchar(255),
  `approval_amount` bigint NOT NULL DEFAULT '0',
  `settlement_reflected_yn` char(1) NOT NULL DEFAULT 'N',
  `raw_data_json` json,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `idx_card_sales_import_approval_date` (`approval_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"employee"	"CREATE TABLE `employee` (
  `id` bigint unsigned NOT NULL auto_increment,
  `employee_no` varchar(50) NOT NULL,
  `password_hash` varchar(255) NOT NULL,
  `resident_registration_no_front` char(6),
  `resident_registration_no_back_enc` varchar(512),
  `name` varchar(100) NOT NULL,
  `english_name` varchar(100),
  `nickname` varchar(100),
  `department_code` varchar(100),
  `position_code` varchar(100),
  `authority_code` varchar(100),
  `phone` varchar(50),
  `email` varchar(255),
  `bank_name` varchar(100),
  `bank_account_no` varchar(255),
  `address` varchar(500),
  `join_date` date,
  `resign_date` date,
  `delete_yn` char(1) NOT NULL DEFAULT 'N',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `idx_employee_authority` (`authority_code`),
  KEY `idx_employee_name` (`name`),
  KEY `idx_employee_phone` (`phone`),
  KEY `uk_employee_employee_no` (`employee_no`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"employee_contract"	"CREATE TABLE `employee_contract` (
  `id` bigint unsigned NOT NULL auto_increment,
  `employee_id` bigint unsigned NOT NULL,
  `written_date` date,
  `contract_start_date` date NOT NULL,
  `contract_end_date` date,
  `annual_salary` bigint,
  `file_path` varchar(1000),
  `delete_yn` char(1) NOT NULL DEFAULT 'N',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `fk_employee_contract_employee` (`employee_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"expense"	"CREATE TABLE `expense` (
  `id` bigint unsigned NOT NULL auto_increment,
  `target_ym` char(6) NOT NULL,
  `category_code` varchar(100),
  `project_code` varchar(100),
  `amount` bigint NOT NULL DEFAULT '0',
  `memo` text,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `idx_expense_target_ym` (`target_ym`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"id_master"	"CREATE TABLE `id_master` (
  `id` bigint unsigned NOT NULL auto_increment,
  `title` varchar(255) NOT NULL,
  `login_id` varchar(255) NOT NULL,
  `use_yn` char(1) NOT NULL DEFAULT 'Y',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"password_master"	"CREATE TABLE `password_master` (
  `id` bigint unsigned NOT NULL auto_increment,
  `authority_code` varchar(100),
  `title` varchar(255) NOT NULL,
  `password_enc` varchar(512) NOT NULL,
  `use_yn` char(1) NOT NULL DEFAULT 'Y',
  `immutable_yn` char(1) NOT NULL DEFAULT 'Y',
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n"sales"	"CREATE TABLE `sales` (
  `id` bigint unsigned NOT NULL auto_increment,
  `target_ym` char(6) NOT NULL,
  `category_code` varchar(100),
  `project_code` varchar(100),
  `amount` bigint NOT NULL DEFAULT '0',
  `memo` text,
  `created_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED,
  `updated_at` datetime NOT NULL DEFAULT 'CURRENT_TIMESTAMP' DEFAULT_GENERATED on update CURRENT_TIMESTAMP,
  `created_by` bigint unsigned,
  `updated_by` bigint unsigned,
  PRIMARY KEY (`id`),
  KEY `idx_sales_target_ym` (`target_ym`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
"\n
