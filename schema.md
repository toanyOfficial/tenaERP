CREATE DATABASE IF NOT EXISTS tenaERP\nCHARACTER SET utf8mb4\nCOLLATE utf8mb4_unicode_ci;\n\nUSE tenaERP;\n\nSET FOREIGN_KEY_CHECKS = 0;\n\n\n\n\n\n/* =========================================================\n   1. 기초코드\n========================================================= */\n\nCREATE TABLE base_code (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n    \n    `group` VARCHAR(100) NOT NULL,\n    `key` VARCHAR(100) NOT NULL,\n    `value` VARCHAR(255) NOT NULL,\n    description TEXT NULL,\n\n    use_yn CHAR(1) NOT NULL DEFAULT 'Y',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL,\n\n    UNIQUE KEY uk_base_code_group_key (`group`, `key`)\n);\n\n\n\n\n\n/* =========================================================\n   2. 인원마스터\n========================================================= */\n\nCREATE TABLE employee (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    employee_no VARCHAR(50) NOT NULL,\n    password_hash VARCHAR(255) NOT NULL,\n\n    resident_registration_no_front CHAR(6) NULL,\n    resident_registration_no_back_enc VARCHAR(512) NULL,\n\n    name VARCHAR(100) NOT NULL,\n    english_name VARCHAR(100) NULL,\n    nickname VARCHAR(100) NULL,\n\n    department_code VARCHAR(100) NULL,\n    position_code VARCHAR(100) NULL,\n    authority_code VARCHAR(100) NULL,\n\n    phone VARCHAR(50) NULL,\n    email VARCHAR(255) NULL,\n\n    bank_name VARCHAR(100) NULL,\n    bank_account_no VARCHAR(255) NULL,\n\n    address VARCHAR(500) NULL,\n\n    join_date DATE NULL,\n    resign_date DATE NULL,\n\n    delete_yn CHAR(1) NOT NULL DEFAULT 'N',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL,\n\n    UNIQUE KEY uk_employee_employee_no (employee_no)\n);\n\n\n\n\n\n/* =========================================================\n   3. 근로계약마스터\n========================================================= */\n\nCREATE TABLE employee_contract (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    employee_id BIGINT UNSIGNED NOT NULL,\n\n    written_date DATE NULL,\n    contract_start_date DATE NOT NULL,\n    contract_end_date DATE NULL,\n\n    annual_salary BIGINT NULL,\n\n    file_path VARCHAR(1000) NULL,\n\n    delete_yn CHAR(1) NOT NULL DEFAULT 'N',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL,\n\n    CONSTRAINT fk_employee_contract_employee\n        FOREIGN KEY (employee_id)\n        REFERENCES employee(id)\n);\n\n\n\n\n\n/* =========================================================\n   4. 계정관리 Header\n========================================================= */\n\nCREATE TABLE account_header (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    url VARCHAR(1000) NOT NULL,\n    title VARCHAR(255) NOT NULL,\n\n    tags_json JSON NULL,\n\n    delete_yn CHAR(1) NOT NULL DEFAULT 'N',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   5. 계정관리 Detail\n========================================================= */\n\nCREATE TABLE account_detail (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    header_id BIGINT UNSIGNED NOT NULL,\n\n    type_code VARCHAR(100) NULL,\n    login_type_code VARCHAR(100) NULL,\n\n    login_id VARCHAR(255) NULL,\n    password_enc VARCHAR(512) NULL,\n\n    authority_code VARCHAR(100) NULL,\n\n    employee_id BIGINT UNSIGNED NULL,\n\n    memo TEXT NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL,\n\n    CONSTRAINT fk_account_detail_header\n        FOREIGN KEY (header_id)\n        REFERENCES account_header(id),\n\n    CONSTRAINT fk_account_detail_employee\n        FOREIGN KEY (employee_id)\n        REFERENCES employee(id)\n);\n\n\n\n\n\n/* =========================================================\n   6. 아이디마스터\n========================================================= */\n\nCREATE TABLE id_master (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    title VARCHAR(255) NOT NULL,\n    login_id VARCHAR(255) NOT NULL,\n\n    use_yn CHAR(1) NOT NULL DEFAULT 'Y',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   7. 비밀번호마스터\n========================================================= */\n\nCREATE TABLE password_master (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    authority_code VARCHAR(100) NULL,\n\n    title VARCHAR(255) NOT NULL,\n\n    password_enc VARCHAR(512) NOT NULL,\n\n    use_yn CHAR(1) NOT NULL DEFAULT 'Y',\n\n    immutable_yn CHAR(1) NOT NULL DEFAULT 'Y',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   8. 지출내역\n========================================================= */\n\nCREATE TABLE expense (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    target_ym CHAR(6) NOT NULL,\n\n    category_code VARCHAR(100) NULL,\n    project_code VARCHAR(100) NULL,\n\n    amount BIGINT NOT NULL DEFAULT 0,\n\n    memo TEXT NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   9. 매출내역\n========================================================= */\n\nCREATE TABLE sales (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    target_ym CHAR(6) NOT NULL,\n\n    category_code VARCHAR(100) NULL,\n    project_code VARCHAR(100) NULL,\n\n    amount BIGINT NOT NULL DEFAULT 0,\n\n    memo TEXT NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   10. 계좌마스터\n========================================================= */\n\nCREATE TABLE bank_account (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    bank_name VARCHAR(100) NOT NULL,\n    account_no VARCHAR(255) NOT NULL,\n    depositor_name VARCHAR(255) NOT NULL,\n\n    authority_code VARCHAR(100) NULL,\n\n    bank_password_enc VARCHAR(512) NULL,\n\n    internet_banking_id VARCHAR(255) NULL,\n    internet_banking_password_enc VARCHAR(512) NULL,\n\n    otp_serial_no VARCHAR(255) NULL,\n\n    project_code VARCHAR(100) NULL,\n\n    memo TEXT NULL,\n\n    use_yn CHAR(1) NOT NULL DEFAULT 'Y',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   11. 은행거래내역\n========================================================= */\n\nCREATE TABLE bank_transaction (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    account_id BIGINT UNSIGNED NOT NULL,\n\n    transaction_datetime DATETIME NOT NULL,\n\n    deposit_amount BIGINT NOT NULL DEFAULT 0,\n    withdraw_amount BIGINT NOT NULL DEFAULT 0,\n\n    balance_amount BIGINT NULL,\n\n    description VARCHAR(1000) NULL,\n\n    settlement_reflected_yn CHAR(1) NOT NULL DEFAULT 'N',\n\n    raw_data_json JSON NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL,\n\n    CONSTRAINT fk_bank_transaction_account\n        FOREIGN KEY (account_id)\n        REFERENCES bank_account(id)\n);\n\n\n\n\n\n/* =========================================================\n   12. 카드매출 적재 테이블\n========================================================= */\n\nCREATE TABLE card_sales_import (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    approval_date DATE NULL,\n    approval_datetime DATETIME NULL,\n\n    card_company VARCHAR(100) NULL,\n\n    merchant_name VARCHAR(255) NULL,\n\n    approval_amount BIGINT NOT NULL DEFAULT 0,\n\n    settlement_reflected_yn CHAR(1) NOT NULL DEFAULT 'N',\n\n    raw_data_json JSON NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   13. 배치로그\n========================================================= */\n\nCREATE TABLE batch_log (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    batch_group VARCHAR(100) NOT NULL,\n    batch_seq BIGINT NOT NULL,\n\n    title VARCHAR(255) NOT NULL,\n\n    executed_date DATETIME NOT NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n\n    UNIQUE KEY uk_batch_log_group_seq (batch_group, batch_seq)\n);\n\n\n\n\n\n/* =========================================================\n   INDEX\n========================================================= */\n\nCREATE INDEX idx_employee_name\nON employee(name);\n\nCREATE INDEX idx_employee_phone\nON employee(phone);\n\nCREATE INDEX idx_employee_authority\nON employee(authority_code);\n\nCREATE INDEX idx_expense_target_ym\nON expense(target_ym);\n\nCREATE INDEX idx_sales_target_ym\nON sales(target_ym);\n\nCREATE INDEX idx_bank_transaction_datetime\nON bank_transaction(transaction_datetime);\n\nCREATE INDEX idx_card_sales_import_approval_date\nON card_sales_import(approval_date);\n\n\n\n\n\nSET FOREIGN_KEY_CHECKS = 1;CREATE DATABASE IF NOT EXISTS tenaERP\nCHARACTER SET utf8mb4\nCOLLATE utf8mb4_unicode_ci;\n\nUSE tenaERP;\n\nSET FOREIGN_KEY_CHECKS = 0;\n\n\n\n\n\n/* =========================================================\n   1. 기초코드\n========================================================= */\n\nCREATE TABLE base_code (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n    \n    `group` VARCHAR(100) NOT NULL,\n    `key` VARCHAR(100) NOT NULL,\n    `value` VARCHAR(255) NOT NULL,\n    description TEXT NULL,\n\n    use_yn CHAR(1) NOT NULL DEFAULT 'Y',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL,\n\n    UNIQUE KEY uk_base_code_group_key (`group`, `key`)\n);\n\n\n\n\n\n/* =========================================================\n   2. 인원마스터\n========================================================= */\n\nCREATE TABLE employee (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    employee_no VARCHAR(50) NOT NULL,\n    password_hash VARCHAR(255) NOT NULL,\n\n    resident_registration_no_front CHAR(6) NULL,\n    resident_registration_no_back_enc VARCHAR(512) NULL,\n\n    name VARCHAR(100) NOT NULL,\n    english_name VARCHAR(100) NULL,\n    nickname VARCHAR(100) NULL,\n\n    department_code VARCHAR(100) NULL,\n    position_code VARCHAR(100) NULL,\n    authority_code VARCHAR(100) NULL,\n\n    phone VARCHAR(50) NULL,\n    email VARCHAR(255) NULL,\n\n    bank_name VARCHAR(100) NULL,\n    bank_account_no VARCHAR(255) NULL,\n\n    address VARCHAR(500) NULL,\n\n    join_date DATE NULL,\n    resign_date DATE NULL,\n\n    delete_yn CHAR(1) NOT NULL DEFAULT 'N',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL,\n\n    UNIQUE KEY uk_employee_employee_no (employee_no)\n);\n\n\n\n\n\n/* =========================================================\n   3. 근로계약마스터\n========================================================= */\n\nCREATE TABLE employee_contract (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    employee_id BIGINT UNSIGNED NOT NULL,\n\n    written_date DATE NULL,\n    contract_start_date DATE NOT NULL,\n    contract_end_date DATE NULL,\n\n    annual_salary BIGINT NULL,\n\n    file_path VARCHAR(1000) NULL,\n\n    delete_yn CHAR(1) NOT NULL DEFAULT 'N',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL,\n\n    CONSTRAINT fk_employee_contract_employee\n        FOREIGN KEY (employee_id)\n        REFERENCES employee(id)\n);\n\n\n\n\n\n/* =========================================================\n   4. 계정관리 Header\n========================================================= */\n\nCREATE TABLE account_header (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    url VARCHAR(1000) NOT NULL,\n    title VARCHAR(255) NOT NULL,\n\n    tags_json JSON NULL,\n\n    delete_yn CHAR(1) NOT NULL DEFAULT 'N',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   5. 계정관리 Detail\n========================================================= */\n\nCREATE TABLE account_detail (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    header_id BIGINT UNSIGNED NOT NULL,\n\n    type_code VARCHAR(100) NULL,\n    login_type_code VARCHAR(100) NULL,\n\n    login_id VARCHAR(255) NULL,\n    password_enc VARCHAR(512) NULL,\n\n    authority_code VARCHAR(100) NULL,\n\n    employee_id BIGINT UNSIGNED NULL,\n\n    memo TEXT NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL,\n\n    CONSTRAINT fk_account_detail_header\n        FOREIGN KEY (header_id)\n        REFERENCES account_header(id),\n\n    CONSTRAINT fk_account_detail_employee\n        FOREIGN KEY (employee_id)\n        REFERENCES employee(id)\n);\n\n\n\n\n\n/* =========================================================\n   6. 아이디마스터\n========================================================= */\n\nCREATE TABLE id_master (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    title VARCHAR(255) NOT NULL,\n    login_id VARCHAR(255) NOT NULL,\n\n    use_yn CHAR(1) NOT NULL DEFAULT 'Y',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   7. 비밀번호마스터\n========================================================= */\n\nCREATE TABLE password_master (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    authority_code VARCHAR(100) NULL,\n\n    title VARCHAR(255) NOT NULL,\n\n    password_enc VARCHAR(512) NOT NULL,\n\n    use_yn CHAR(1) NOT NULL DEFAULT 'Y',\n\n    immutable_yn CHAR(1) NOT NULL DEFAULT 'Y',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   8. 지출내역\n========================================================= */\n\nCREATE TABLE expense (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    target_ym CHAR(6) NOT NULL,\n\n    category_code VARCHAR(100) NULL,\n    project_code VARCHAR(100) NULL,\n\n    amount BIGINT NOT NULL DEFAULT 0,\n\n    memo TEXT NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   9. 매출내역\n========================================================= */\n\nCREATE TABLE sales (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    target_ym CHAR(6) NOT NULL,\n\n    category_code VARCHAR(100) NULL,\n    project_code VARCHAR(100) NULL,\n\n    amount BIGINT NOT NULL DEFAULT 0,\n\n    memo TEXT NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   10. 계좌마스터\n========================================================= */\n\nCREATE TABLE bank_account (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    bank_name VARCHAR(100) NOT NULL,\n    account_no VARCHAR(255) NOT NULL,\n    depositor_name VARCHAR(255) NOT NULL,\n\n    authority_code VARCHAR(100) NULL,\n\n    bank_password_enc VARCHAR(512) NULL,\n\n    internet_banking_id VARCHAR(255) NULL,\n    internet_banking_password_enc VARCHAR(512) NULL,\n\n    otp_serial_no VARCHAR(255) NULL,\n\n    project_code VARCHAR(100) NULL,\n\n    memo TEXT NULL,\n\n    use_yn CHAR(1) NOT NULL DEFAULT 'Y',\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   11. 은행거래내역\n========================================================= */\n\nCREATE TABLE bank_transaction (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    account_id BIGINT UNSIGNED NOT NULL,\n\n    transaction_datetime DATETIME NOT NULL,\n\n    deposit_amount BIGINT NOT NULL DEFAULT 0,\n    withdraw_amount BIGINT NOT NULL DEFAULT 0,\n\n    balance_amount BIGINT NULL,\n\n    description VARCHAR(1000) NULL,\n\n    settlement_reflected_yn CHAR(1) NOT NULL DEFAULT 'N',\n\n    raw_data_json JSON NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL,\n\n    CONSTRAINT fk_bank_transaction_account\n        FOREIGN KEY (account_id)\n        REFERENCES bank_account(id)\n);\n\n\n\n\n\n/* =========================================================\n   12. 카드매출 적재 테이블\n========================================================= */\n\nCREATE TABLE card_sales_import (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    approval_date DATE NULL,\n    approval_datetime DATETIME NULL,\n\n    card_company VARCHAR(100) NULL,\n\n    merchant_name VARCHAR(255) NULL,\n\n    approval_amount BIGINT NOT NULL DEFAULT 0,\n\n    settlement_reflected_yn CHAR(1) NOT NULL DEFAULT 'N',\n\n    raw_data_json JSON NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n    created_by BIGINT UNSIGNED NULL,\n    updated_by BIGINT UNSIGNED NULL\n);\n\n\n\n\n\n/* =========================================================\n   13. 배치로그\n========================================================= */\n\nCREATE TABLE batch_log (\n    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n\n    batch_group VARCHAR(100) NOT NULL,\n    batch_seq BIGINT NOT NULL,\n\n    title VARCHAR(255) NOT NULL,\n\n    executed_date DATETIME NOT NULL,\n\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n\n    UNIQUE KEY uk_batch_log_group_seq (batch_group, batch_seq)\n);\n\n\n\n\n\n/* =========================================================\n   INDEX\n========================================================= */\n\nCREATE INDEX idx_employee_name\nON employee(name);\n\nCREATE INDEX idx_employee_phone\nON employee(phone);\n\nCREATE INDEX idx_employee_authority\nON employee(authority_code);\n\nCREATE INDEX idx_expense_target_ym\nON expense(target_ym);\n\nCREATE INDEX idx_sales_target_ym\nON sales(target_ym);\n\nCREATE INDEX idx_bank_transaction_datetime\nON bank_transaction(transaction_datetime);\n\nCREATE INDEX idx_card_sales_import_approval_date\nON card_sales_import(approval_date);\n\n\n\n\n\nSET FOREIGN_KEY_CHECKS = 1;
